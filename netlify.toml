[build]
  base    = "app"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback — all unknown routes serve index.html so react-router can take over
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
